import React from 'react';
import Bar from './bar';

const Home = (props) => {

    return (
        <div className="Root" >
            <Bar />
        </div>
    );
}

export default Home;
